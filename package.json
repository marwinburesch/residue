{
  "name": "residue",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/ui/server.ts",
    "build": "bun build src/main.ts --outdir dist --target browser --minify",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
